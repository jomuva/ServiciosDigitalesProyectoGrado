@using ServiciosDigitalesProy.Models
@model ServiciosDigitalesProy.Models.Usuario
@{
    ViewBag.Title = " Usuarios";
    ViewBag.SubSeccion = "Modificar Estado del Empleado";
}

<h3>@ViewBag.SubSeccion</h3>
<div class="col-lg-12">
    <!--breadcrumbs start -->
    <ul class="breadcrumb">
        <li><a href="#"><i class="fa fa-th-list"></i>@ViewBag.Title</a></li>
        <li class="active"><a href="#">@ViewBag.SubSeccion</a></li>
    </ul>
    <!--breadcrumbs end -->
</div>

<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-6">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Datos del Empleado
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-lg-6">
                            @using (Html.BeginForm("ModificarEstadoEmpleado", "Usuarios", FormMethod.Post))
                            {
                                <div class="form-group">
                                    <label>No Identificación</label>
                                    @Html.TextBox("identificacion", Model.identificacion, new { @class = "form-control", @disabled = "disabled"})
                                </div>
                                @Html.Hidden("identificacion", Model.identificacion)

                                <div class="form-group">
                                    <label>Nombre Cliente</label>
                                    @Html.TextBox("identificacion",Model.nombres, new { @class = "form-control", @disabled = "disabled" })
                                </div>

                                <div class="form-group">
                                    <label>Estado</label>
                                    @Html.DropDownListFor(x => x.idEstado, Model.tiposEstado, null, new { @class = "form-control" })
                                </div>

                                <button type="submit" name="submitButton" id="submitButton" class="btn btn-primary">Aceptar</button>
                            }
                        </div>
                    </div>
                    <!-- /.row (nested) -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-6 -->
    </div>
    <!-- /.row -->
</div>